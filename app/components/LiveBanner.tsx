'use client';

export default function LiveBanner() {
  return (
    <div className="bg-red-600 text-white py-2 px-4 text-center font-semibold text-sm animate-pulse">
      🔴 LIVE NOW - Watch the stream above!
    </div>
  );
}
